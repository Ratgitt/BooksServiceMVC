spring:
  application:
    name: project2_SpringBoot
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://project2-postgres:5432/project2
#    url: jdbc:postgresql://localhost:5434/project2
    username: postgres
    password: ratsql
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  mvc:
    hidden method:
      filter:
        enabled: true